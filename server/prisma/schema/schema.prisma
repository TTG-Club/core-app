generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters", "fullTextIndex", "fullTextSearch", "prismaSchemaFolder", "omitApi"]
}

datasource db {
  provider     = "mysql"
  url          = env("NUXT_MYSQL_URI")
  relationMode = "prisma"
}
