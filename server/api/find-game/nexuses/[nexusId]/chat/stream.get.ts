/**
 * SSE-лента чата комнаты. Маршрут более специфичный, чем `/api/find-game/**`,
 * поэтому Nitro отдаёт поток сюда, а не в обычный прокси: обычный буферизует
 * ответ и держит подписку у сервиса после ухода клиента.
 */
export default defineEventHandler(proxyFindGameStream);
