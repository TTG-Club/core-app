/**
 * Все обычные запросы раздела поиска игр. Общий `/api/**` уводит незнакомые
 * пути в core-api, поэтому у find-game-api свой префикс и свой обработчик.
 * SSE-ленты чата обслуживают отдельные, более специфичные маршруты рядом.
 */
export default defineEventHandler(proxyFindGame);
