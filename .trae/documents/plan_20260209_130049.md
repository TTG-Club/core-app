I have analyzed the folder `/Users/svifty7/Projects/_ttg-club/core-app/app/features/calculator/abilities` and identified a few violations of the `.cursorrules`. Here is the plan to fix them.

### Violations Found

1. **Single Letter Variables (Strict Violation):**
   - `ui/BonusFeats.vue`: `b` (loop variable), `k` (callback), `lvl` (abbreviation), `cls` (abbreviation).

   - `ui/components/BonusBackgrounds.vue`: `bg` (abbreviation).

   - `ui/tabs/StandardArray.vue`: `v` (callback).

2. **Template Hygiene (Strict Violation):**
   - `ui/tabs/StandardArray.vue`: Inline function with logic `(v) => updateScore(key, Number(v))` in template.

3. **Semantic Colors (Strict Violation):**
   - `ui/tabs/PointBuy.vue`: Uses `text-red-500` instead of `text-error`.

   - `ui/tabs/StandardArray.vue`: Uses `text-red-500` instead of `text-error`.

### Implementation Plan

#### 1. Refactor `ui/BonusFeats.vue`

- Rename `b` to `background` in loop.

- Rename `k` to `key` in map callback.

- Rename `lvl` to `level` to avoid abbreviation.

- Rename `cls` to `classItem` to avoid abbreviation.

#### 2. Refactor `ui/components/BonusBackgrounds.vue`

- Rename `bg` variable to `background`.

#### 3. Refactor `ui/tabs/StandardArray.vue`

- Extract inline handler `(v) => updateScore(key, Number(v))` to a named function `handleScoreUpdate(key, value)`.

- Rename `v` to `value`.

- Replace `text-red-500` with `text-error`.

#### 4. Refactor `ui/tabs/PointBuy.vue`

- Replace `text-red-500` with `text-error`.

#### 5. Verification

- Run `pnpm run lint:write` to format code.

- Run `pnpm run type-check` to ensure no type errors were introduced.

\
Импорты все еще не соответствуют правилам, в папках не хватает index.ts с реэкспортами, как указано в правилах
