/** Подпись панели: последняя созданная игра из каталога */
export const HOME_LATEST_GAME_LABEL = 'Новая игра';

/** Иконка панели */
export const HOME_LATEST_GAME_ICON = 'tabler:users-plus';

/** Подпись перехода в каталог игр */
export const HOME_LATEST_GAME_ALL_LABEL = 'Все игры';

/** Заглушка, если в каталоге нет ни одной игры */
export const HOME_LATEST_GAME_EMPTY_TEXT = 'Игр в каталоге пока нет';

/** Заглушка, если сервис игр не ответил */
export const HOME_LATEST_GAME_ERROR_TEXT = 'Не удалось загрузить игры';

/** Ключ `useAsyncData` выдачи каталога для блока */
export const HOME_LATEST_GAME_DATA_KEY = 'home-latest-game';

/**
 * Страница каталога, в которой ищем последнюю созданную игру, — первая
 * (сервис считает страницы с нуля)
 */
export const HOME_LATEST_GAME_LOOKUP_PAGE = 0;

/**
 * Сколько игр каталога просматривать в поисках последней созданной.
 *
 * Сортировки по дате создания у сервиса нет: выдача идёт по позиции в
 * списке, а мастер может поднять старую игру наверх. Новая игра встаёт в
 * список по времени создания, поэтому выше неё оказываются только игры,
 * поднятые позже, — одной страницы с запасом хватает, чтобы она в неё попала.
 */
export const HOME_LATEST_GAME_LOOKUP_SIZE = 20;
