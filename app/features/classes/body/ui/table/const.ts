/** Прочерк в круге без ячеек. */
export const EMPTY_SPELL_SLOT_CELL = '—';

/** Подсветка строки ячеек Магии договора: они не входят в общий запас. */
export const PACT_MAGIC_ROW_CLASS = 'bg-accented/30';

/** Подписи таблицы ячеек заклинаний сборки мультикласса. */
export const MULTICLASS_SPELL_SLOT_TABLE_LABELS = {
  spellcasterLevel: 'Уровень заклинателя',
  pactMagicNote:
    'Ячейки Магии договора считаются отдельно, по уровню колдуна, и в общий запас не входят. Они восстанавливаются коротким отдыхом; тратить их можно и на заклинания других классов, а общие ячейки — на заклинания колдуна.',
};
